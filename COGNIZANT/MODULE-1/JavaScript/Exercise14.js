$("#registerBtn").click(function () {
  $("#eventCard").fadeIn();
  setTimeout(() => $("#eventCard").fadeOut(), 3000);
});

// Benefit of React/Vue: Component-based architecture makes code modular, scalable, and easier to manage.
